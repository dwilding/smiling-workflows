repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-merge-conflict
      - id: trailing-whitespace
      - id: mixed-line-ending
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-toml
  - repo: local
    hooks:
      - id: zizmor
        name: Run zizmor
        entry: bash -c 'uvx zizmor@1.25.2 --format=sarif . > workflows.sarif'
        language: system
        pass_filenames: false
        always_run: true
